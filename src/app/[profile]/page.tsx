export default function Profile({ params }: { params: { profile: string } }) {
    return (
        <>
            <p>{params.profile}</p>
        </>
    );
}

// ... menu for shairing in top right

// PFP

// Username (perhaps with verified icon)

// About

// Links to socials

// custom links
